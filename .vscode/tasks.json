{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run go",
            "type": "shell",
            "command": "air -- -path ../test-folder",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ,
    {
        "label": "Run go test",
        "type": "shell",
        "command": "go test ./tests -v",
        "options": {
            "cwd": "${workspaceFolder}/src"
        },
        "problemMatcher": [],
        "group": {
            "kind": "test",
            "isDefault": true
        }
    }
    ]
}